#include <stdio.h>

#define SIZE 2

int main()
{
    int user_input[SIZE];
    int i;

    printf("Please enter %d numbers and I will find the HCF of them", SIZE);

    for(i = 0; i < SIZE; i++)
    {
        scanf("%d", user_input[i]);
    }

    for(i = 0; i < SIZE; i++)
    {
        user_input[i]
    }


    return 0;
}
